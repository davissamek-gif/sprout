# Sprout (eco-life-pwa)

Mobilní webová appka (PWA-ready) pro plánování, mapu a společný deník (private-first).

## Start
1) `npm install`
2) `npm run dev`

## Poznámka
Firebase config doplníme do `.env.local` (další krok).
